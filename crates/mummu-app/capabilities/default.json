{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell's own windows. The chat UI is plain HTML served over loopback and uses no Tauri IPC, so nothing beyond the core defaults is granted.",
  "windows": ["main", "splash"],
  "permissions": ["core:default"]
}
