# Rust
target/
**/*.rs.bk
*.pdb

# model weights / caches (fetched on demand, never committed)
/models
*.safetensors
*.gguf

# tauri-build writes the capability JSON schemas here on every build
/crates/mummu-app/gen

# OS / editor
.DS_Store
Thumbs.db
